version: '3.8'

services:
  whatsapp-crm:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
